import type { DocumentCardData } from "@/lib/documents";
import DocumentManageActions from "./DocumentManageActions";

export default function DocumentManageCard({ card }: { card: DocumentCardData }) {
  return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-700 dark:bg-slate-900"><div className="flex items-start justify-between gap-3"><span className="rounded-full bg-blue-100 px-3 py-1 text-xs font-semibold text-blue-700 dark:bg-blue-950/70 dark:text-blue-300">Toán · {card.grade}</span><span className={`rounded-full px-2.5 py-1 text-[11px] font-bold ${card.status === "published" ? "bg-emerald-100 text-emerald-700" : "bg-amber-100 text-amber-700"}`}>{card.status === "published" ? "Đã đăng" : "Bản nháp"}</span></div><h2 className="mt-4 text-lg font-bold text-slate-900 dark:text-white">{card.title}</h2><p className="mt-2 line-clamp-2 text-sm text-slate-600 dark:text-slate-400">{card.description || "Tài liệu học tập môn Toán."}</p><div className="mt-3 flex flex-wrap gap-1.5">{card.topics.map(topic => <span key={topic.id} className="rounded-full bg-violet-50 px-2.5 py-1 text-[11px] font-semibold text-violet-700 dark:bg-violet-950/60 dark:text-violet-300">{topic.name}</span>)}</div>{card.status === "published" && <a href={`/tai-lieu/${card.id}`} className="mt-4 inline-block text-sm font-semibold text-indigo-600 hover:underline">Xem tài liệu →</a>}<DocumentManageActions id={card.id} status={card.status} storagePaths={card.imageStoragePaths} /></div>;
}
